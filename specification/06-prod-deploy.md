Read AGENT.md to understand the project.

Read https://flask.palletsprojects.com/en/stable/deploying/gunicorn/  

Modify Dockerfile to deploy the flask application to gunicorn.
Number of worker must be configurable via an environment variable called FIND_WEB_WORKERS with a default of 4.

Update AGENT.md accordingly.

